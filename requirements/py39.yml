name: cf-units-dev

channels:
  - conda-forge

dependencies:
  - python 3.9

# setup dependencies
  - cython
  - numpy
  - pytest-runner

# core dependencies
  - antlr-python-runtime 4.7.2.*
  - cftime>=1.2
  - numpy
  - udunits2

# test dependencies
  - coveralls
  - jinja2
  - pep8
  - pytest
  - pytest-cov

# dev dependencies
  - pip
