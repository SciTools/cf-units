name: cf-units-dev

channels:
  - conda-forge

dependencies:
# setup dependencies
  - cython
  - numpy

# core dependencies
  - antlr-python-runtime 4.7.2.*
  - cftime>=1.2
  - numpy
  - udunits2

# test dependencies
  - codecov
  - jinja2
  - pytest
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit

# docs dependencies
  - setuptools_scm
  - sphinx
