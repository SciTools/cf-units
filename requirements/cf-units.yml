name: cf-units-dev

channels:
  - conda-forge

dependencies:
# setup dependencies
  - setuptools >=61
  - setuptools_scm >=7
  - cython
  - numpy

# core dependencies
  - antlr-python-runtime 4.7.2.*
  - cftime>=1.5.2
  - numpy
  - udunits2

# test dependencies
  - codecov
  - jinja2
  - pytest
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit

# docs dependencies
  - sphinx
