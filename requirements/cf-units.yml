name: cf-units-dev

channels:
  - conda-forge

dependencies:
# setup dependencies
  - setuptools >=77.0.3
  - setuptools_scm >=8.0
  - cython >=3.0
  - numpy

# core dependencies
  - cftime>=1.2
  - numpy>=1.23
  - udunits2

# test dependencies
  - codecov
  - jinja2
  - pytest
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit

# docs dependencies
  - sphinx
# latex
  - antlr-python-runtime 4.11.1.*  # To update this, see cf_units/_udunits2_parser/README.md
